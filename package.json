{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "predeploy": "npm run build",
        "deploy": "gh-pages -d build"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "gh-pages": "^6.1.1",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    }
}
